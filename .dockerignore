# The image build only needs Cargo.toml, Cargo.lock, src/ and static/.
# Everything else is context-transfer overhead or must never reach the image.
/.git
/.github
/.claude
/target
/tmp
/docs
/e2e
/tests
/examples
/scripts
/.env
*.db
*.sqlite
*.sqlite3
*.mmdb
*.log
.DS_Store
